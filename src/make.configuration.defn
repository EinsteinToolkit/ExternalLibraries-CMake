# make.configuration.defn file for thorn CMake

# Define standard CMake utilities and install those that we could build/that are
# present in the system
STD_CMAKE_UTILS = 
ALL_UTILS += $(STD_CMAKE_UTILS)
